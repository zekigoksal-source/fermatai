# ── Eyotek LMS Scraping ───────────────────────────────────────────────────────
asyncpg>=0.29.0
httpx>=0.27.0
playwright>=1.44.0
python-dotenv>=1.0.0
loguru>=0.7.0

# ── AI / LLM ─────────────────────────────────────────────────────────────────
anthropic>=0.25.0          # Claude API — tool-calling, pedagojik muhakeme
openai>=1.0.0              # Whisper API — ses transkripsiyon

# ── WhatsApp Bridge ───────────────────────────────────────────────────────────
fastapi>=0.110.0           # Webhook sunucusu
uvicorn[standard]>=0.29.0  # ASGI server

# ── External APIs (23 Nisan Jarvis paket) ────────────────────────────────────
google-auth>=2.28.0                # Google Calendar service account
google-api-python-client>=2.120.0  # Calendar event CRUD

# ── Visual Generator ─────────────────────────────────────────────────────────
matplotlib>=3.8.0          # Fonksiyon grafiği + fizik çizimi
numpy>=1.26.0              # plot_function için
groq>=0.15.0              # Llama 3.3 70B — ucuz/hizli inference
nest_asyncio>=1.6.0       # Async re-entry Groq sync wrapper

# ── Oturum 25.38 — Anki .apkg export + Sentry error tracking ────────────────
genanki>=0.13.0           # Anki .apkg deck üretimi (öğrenci telefonuna kart)
sentry-sdk[fastapi]>=2.0.0  # Production error tracking (DSN env varsa aktif)
